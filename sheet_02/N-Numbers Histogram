#include <iostream>
 
using namespace std;
 
int main()
{
	char s;
	cin >> s;
	int n, a;
	cin >> n;
	while (n--)
	{
		cin >> a;
		if (s == '+'||s=='-'||'*'||'/')
		{
			for (int i = 0; i < a; i++)
			{
				cout << s;
			}
			cout<<endl;
		}
	}
	
}
